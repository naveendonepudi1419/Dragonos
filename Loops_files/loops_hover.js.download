var videos = document.getElementsByClassName('shape-video');


for ( i=0; i<videos.length; i++) {

	videos[i].onmouseenter = function ( event ) {
		let target = event.target;
		target.play();
	};


	videos[i].onmouseout = function(event) {
		let target = event.target;
		target.pause();
		
	};

};
